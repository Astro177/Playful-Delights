import React from 'react';
import useTitle from '../hooks/useTitle';

const AboutUs = () => {
    useTitle("About Us")
    return (
        <div>
            This is about page
        </div>
    );
};

export default AboutUs;